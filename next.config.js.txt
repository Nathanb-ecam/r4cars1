/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [{ protocol: 'https', hostname: '**' }],
    domains: ['localhost','172.20.144.1','placehold.co'],
  },
  // No i18n field for App Router!
};

module.exports = nextConfig; 

